--!strict
-- ScareService: шкала храбрости и очки страха. Считает ТОЛЬКО сервер.
-- Клиент шлёт позицию/взгляд; сервер проверяет дистанцию до монстра и кулдауны.
-- Урон храбрости — в % от максимума. Эффекты скримера — на клиенте, урон — здесь.

local ScareService = {}

function ScareService.Init()
	-- TODO Неделя 3–4:
	-- 1. Шкала храбрости посетителя (100%), восстановление со временем
	-- 2. Триггеры испуга: сервер валидирует дистанцию, кулдаун монстра
	-- 3. Храбрость = 0 → «побег»: телепорт к выходу, максимум очков владельцу
	-- 4. Анти-абьюз: сбежал до середины → 0 очков; тир жертвы сильно ниже → ×0.1
	-- 5. Экскурсионный режим: первые GameConfig.Scare.TourModeVisits визитов пол храбрости 10%
	print("[ScareService] init")
end

return ScareService
